# V28 Performance

V28 没有修改 V27 的路线运动、镜头和标签渲染算法。新增功能只在编辑操作、空闲保存或打开弹窗时运行，不进入逐帧成片渲染路径。

## 自动化测量

环境：Chromium Headless、SwiftShader 软件 WebGL、1440×900，连续播放约 4.5 秒。

| 指标 | 结果 |
|---|---:|
| 采样帧 | 162 |
| Canvas 平均绘制 | 2.09 ms |
| Canvas 最大绘制 | 7.5 ms |
| 自适应分辨率 | 正常工作 |
| 最大进度步长 | 0.003423 |
| Runtime 错误 | 0 |

软件 WebGL 环境的帧间隔约 33.3 ms；这主要反映无硬件 GPU 的自动化环境，不代表本地 Chrome / Edge 的硬件加速帧率。新增恢复点使用 `requestIdleCallback` 或延迟任务执行，避免占用播放关键帧。

## 存储保护

- 最多保留 12 个本地恢复点。
- 写入失败时自动删除更旧恢复点重试。
- 单个项目快照超过约 1.2 MB 时，不重复保存照片数据，只保存路线和设置，并在历史中标记 `route only`。
